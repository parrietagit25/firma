# Configuración para agregar a tu servidor Nginx existente
# Agregar este bloque dentro del server {} de tu configuración principal

# Proxy para el generador de firmas en la ruta /generador_firmas
location /generador_firmas {
    proxy_pass http://localhost:8084/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Configuración para archivos estáticos
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg)$ {
        proxy_pass http://localhost:8084;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# Redirigir /generador_firmas/ a /generador_firmas
location = /generador_firmas/ {
    return 301 /generador_firmas;
}
